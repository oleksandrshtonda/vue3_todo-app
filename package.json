{
  "name": "vue3_todo-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",

    "prepare-to-deploy": "chmod +x deploy.sh",
    "run-deploy-script": "deploy.sh",
    "deploy": "npm run prepare-to-deploy && npm run run-deploy-script"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.5.1",
    "bulma": "^0.9.4",
    "sass": "^1.71.0",
    "vue": "^3.4.15"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.3",
    "vite": "^5.0.11"
  }
}
